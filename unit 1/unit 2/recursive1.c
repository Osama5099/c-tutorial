#include<stdio.h>
int fib_recrsive(int n)
{
    return 0;
}
int fib_itretive(int n0)
{
    return 0;
}
int main()
{
    int number;
    printf("Enter a number \n");
    scanf("%d",&number);
    
    printf("The vlue of fibonacci number at position no %d using itretive approch is %d\n",number,fib_itretive(number));
    printf("The vlue of fibonacci number at position no %d using itretive approch is %d\n",fib_recrsive(number));
    return 0;
}